# QCOW2/raw disk: root + /boot required.
# Demo user comes from container (build.sh). Log in as demo/zaatar.
[[customizations.filesystem]]
mountpoint = "/boot"
minsize = "512 MiB"

[[customizations.filesystem]]
mountpoint = "/"
minsize = "10 GiB"
