# QCOW2/raw disk: root + /boot required.
# Types come from workflow rootfs: btrfs (root) and builder default for /boot.
[[customizations.filesystem]]
mountpoint = "/boot"
minsize = "512 MiB"

[[customizations.filesystem]]
mountpoint = "/"
minsize = "10 GiB"
